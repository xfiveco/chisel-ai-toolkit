# Agent instructions

This project's agent instructions live in [`CLAUDE.md`](CLAUDE.md). Read that file first — it is the single source of truth for any AI coding agent working on this theme.

Supporting material, all relative to the theme root (`{{THEME_ROOT}}`):

- `.claude/chisel/reference/` — descriptive rules: file locations, decision ladders, conventions.
- `.claude/chisel/templates/` — code scaffolds to copy.
- `.claude/skills/chisel-*/SKILL.md` — step-by-step procedures. Agents without skill support can read these as plain markdown.
